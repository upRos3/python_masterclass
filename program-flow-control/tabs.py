# Python uses indentation as part of the language.

for i in range(1, 12):
    # this print method is a 'block'
    print("No. {:2} squared is {:4} and cubed is {:4}".format(i, i**2, i**4))

# You close a block off by coming back to column one of the next line

